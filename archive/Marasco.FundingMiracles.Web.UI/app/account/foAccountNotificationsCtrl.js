fundoloApp.controller('foAccountNotificationsCtrl', [
  '$scope',
  function ($scope) {

    //#region === Initialize ===

    'use strict';
    $scope.$parent.pageResolve = 'notifications';

    //endregion
  }
]);